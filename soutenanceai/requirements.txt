# Core Django
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0

# WebSocket / temps réel
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# Base de données
# psycopg2-binary==2.9.9  ← pas de wheel Python 3.13 ; inutile en SQLite (dev)
# Pour PostgreSQL en prod : pip install "psycopg2-binary>=2.9.10" ou psycopg[binary]

# Environnement
python-dotenv==1.0.1
django-environ==0.11.2

# IA & API externes
anthropic==0.39.0
groq==0.13.0

# Traitement fichiers
PyMuPDF==1.24.13
python-pptx==1.0.2
Pillow==10.4.0

# Génération PDF
reportlab==4.2.5

# Audio
pydub==0.25.1

# Utilitaires
requests==2.32.3

# Tests
pytest==8.3.3
pytest-django==4.9.0
pytest-asyncio==0.24.0

# Export Excel (plannings, notes)
openpyxl==3.1.5

# Compilation des traductions sans GNU gettext (Windows)
polib==1.2.0
